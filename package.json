{
    "scripts": {
        "start": "concurrently 'npm start --prefix ./api' 'npm start --prefix ./client'",
        "install-dev": "./scripts/install.sh"
    },
    "devDependencies": {
        "concurrently": "^5.3.0"
    }
}
